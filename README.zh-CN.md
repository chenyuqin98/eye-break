# eye-break

[English](README.md) · **中文**

macOS 上的 20-20-20 护眼提醒，按**实际用眼时间**计时，而不是墙上时钟。

> 20-20-20 法则：每用眼 20 分钟，看 20 英尺（约 6 米）外的东西 20 秒。
> 目的是放松睫状肌 —— 长时间近距离对焦累的就是它。

不需要 Homebrew，不是 Electron，也不占菜单栏。几个 shell 脚本 + 一个 40 KB 的
AppleScript 通知器 + 一个 80 KB 的 Swift 全屏遮罩 + 一个 launchd 任务，
所有东西都在 `~/.eye-break` 里。

## 为什么不直接设个 20 分钟定时器

因为固定定时器不知道你有没有真的在看屏幕。第 5 分钟你锁屏走了，第 19 分钟回来，
定时器会在你看了 1 分钟屏幕之后就来烦你 —— 而且之后一整天的节奏都是错位的。

`eye-break` 每分钟 tick 一次，只累加你真的在电脑前的时间：

| 情况 | 行为 |
|---|---|
| 正常使用（空闲 < 90 秒） | 正常累加 |
| 短暂发呆（90 秒 ~ 5 分钟） | **暂停累加** —— 你可能只是在读屏幕 |
| 锁屏超过 20 秒 | **清零** —— 这本身就已经是那 20 秒远眺 |
| 锁屏不到 20 秒 | **暂停累加**，进度保留 |
| 没锁屏但 2 分钟没碰键鼠 | **清零** |
| 合盖睡眠后唤醒 | **清零** —— 不会一坐下就被提醒 |
| 攒够了但不在活跃时段 | 跳过并记日志，下一轮从头开始 |

锁屏状态直接读 IORegistry 里的 `IOConsoleLocked`，**不是**用空闲时间推断的。
这一点很关键：锁屏期间 `HIDIdleTime` 会被锁屏界面自己不断重置，你人不在的整段
时间里它都贴着 0，所以任何基于空闲时间的判断都会以为你从没离开过。

## 休息长什么样

通知太容易错过了，看过几百次之后更是条件反射地忽略。所以真正的休息是一整块遮罩：

```
┌────────────────────────────────────────────────┐
│                                                │
│              👀  看向 6 米外                    │
│                                                │
│                    17                          │
│              ●●●●●●●●●●●○○○○○○○○○              │
│                                                │
│           睫状肌绷了 20 分钟了                   │
│                                                │
│              按 esc 跳过这次                    │
│                                                │
└────────────────────────────────────────────────┘
```

它盖在菜单栏和全屏应用之上，每块屏幕都盖，并且吞掉所有按键 —— 你没法对着
底下的窗口继续打字。`BREAK_SECONDS` 之后自己消失。

两点让它不至于烦人：

- **按 `esc` 可以跳过**。跳过会被记下来，所以遵守率不会虚高。
- **演示时它自动退让**。投屏、Keynote 演示、播视频都会持有
  `PreventUserIdleDisplaySleep`，检测到就只发通知不遮罩 —— 演示到一半
  被全屏盖住是最该被工程手段避免的翻车。代价是看视频时也不遮罩；
  想要就把 `OVERLAY_SKIP_WHEN_PRESENTING` 设成 `0`。

只想要通知就把 `OVERLAY` 设成 `0`。

## 安装

```sh
git clone https://github.com/chenyuqin98/eye-break.git
cd eye-break
./install.sh --lang zh        # 不加 --lang 则跟随系统语言
```

第一次通知可能需要授权。通知器是一个有独立身份的真 App，所以会作为「护眼提醒」
出现在**系统设置 → 通知**里。建议把样式从「横幅」改成「提醒」—— 提醒不会自动消失，
要手动点掉，对这种「必须真的离开屏幕」的场景更管用。

## 用法

```sh
~/.eye-break/eye-break.sh --status     # 已累计用眼 7 分 1 秒 / 20 分，还差 12 分 59 秒
                                       # 今天 09:12 起坐到屏幕前，累计用眼 3 小时 40 分，收到 9 次提醒
~/.eye-break/eye-break.sh --stats      # 最近 7 天的每日统计
~/.eye-break/eye-break.sh --stats 30   # 最近 30 天
~/.eye-break/eye-break.sh --now        # 立刻发一轮提醒
~/.eye-break/eye-break.sh --help
```

## 每日统计

```
  20-20-20 护眼统计 · 最近 7 天
  ──────────────────────────────────────────────────────────────────
  日期         用眼时间   离屏     提醒  照做  遵守率  中断   用眼强度
  2026-08-14   5h20m      1h04m    16     11    68%     7      ████████··
  2026-08-15   6h40m      52m      20     13    65%     9      ██████████
  2026-08-16   1h20m      18m      4      4     100%    2      ██········
  ──────────────────────────────────────────────────────────────────
  合计  3 天   13h20m     2h14m    40     28    70%
  日均         4h26m      44m      13 次提醒
```

| 列 | 含义 |
|---|---|
| **用眼时间** | 计入 20 分钟配额的时间，也就是你真正盯屏幕的时长 |
| **离屏** | 锁屏 + 长时间没碰键鼠，只统计活跃时段内的 |
| **提醒** | 实际发出的提醒次数 |
| **照做** | 其中遮罩整段走完、没按 `esc` 的次数 |
| **遵守率** | 照做 / 提醒 |
| **中断** | 计时器被清零了几次（锁屏、空闲或机器睡眠） |

**「照做」怎么测的。**开着遮罩时，用的就是遮罩自己的退出码：整段走完，还是你
按了 `esc`。这是实打实的信号，不是猜的。`OVERLAY=0` 时才回落到代理指标 ——
整段没碰键鼠 —— 那个指标看不到你眼睛朝哪儿，坐着不动盯着同一块屏幕也会算通过。

数据按天存成 `~/.eye-break/daily/YYYY-MM-DD`，每个 tick 重写一次，崩溃最多丢一分钟。
从没有统计功能的旧版本升级时，`install.sh` 会调 `backfill.sh` 把旧日志能证明的部分
捞回来：提醒次数和锁屏时长是精确的，用眼时间只能给出下限（提醒次数 × `INTERVAL`），
这些天会标上 `*` 并且不计入遵守率。

## 配置

编辑 `~/.eye-break/config`，下一次 tick 就生效，不用重载。

| 配置项 | 默认值 | 含义 |
|---|---|---|
| `LANG_PREF` | `auto` | `auto` / `zh` / `en`，`auto` 跟随系统语言 |
| `ACTIVE_START` / `ACTIVE_END` | `9` / `23` | 只在这个时段提醒 |
| `INTERVAL` | `1200` | 攒够多少秒用眼时间提醒一次 |
| `BREAK_RESET` | `120` | 没锁屏但这么久没碰键鼠就清零 |
| `SLEEP_GAP` | `300` | tick 间隔这么大就判定机器睡过（下限 180） |
| `LOCK_RESET` | `20` | 锁屏超过这么久就清零 |
| `IDLE_PAUSE` | `90` | 空闲超过这么久就暂停累加 |
| `BREAK_SECONDS` | `20` | 远眺时长 |
| `OVERLAY` | `1` | `1` = 休息时全屏遮罩，`0` = 只发通知 |
| `OVERLAY_OPACITY` | `0.94` | `0.3`（几乎透明）… `1.0`（纯黑） |
| `OVERLAY_SKIP_WHEN_PRESENTING` | `1` | 有东西撑着屏幕不休眠时不抢屏 |
| `START_SOUND` / `END_SOUND` | `Glass` / `Ping` | `/System/Library/Sounds` 里的任意名字 |

每次提醒从文案池里随机抽，中英各有 14 条开场 + 8 条收尾。想加自己的词，直接往
`eye-break.sh` 顶部的数组里塞，格式是 `"标题|正文"`。

## 工作原理

```
launchd（每 60 秒） -> eye-break.sh -> 从 ioreg 读 HIDIdleTime
                            |
                            +   以及 ioreg 的 IOConsoleLocked
                            |
                            +-- tick间隔>=SLEEP_GAP .. 睡过了：清零，退出
                            +-- 锁屏中 .............. 累加锁屏时长；
                            |                         >= 20 秒 -> 清零，退出
                            +-- 空闲 >= 120 秒 ...... 清零，退出
                            +-- 空闲 >= 90 秒 ....... 暂停，退出
                            +-- 否则 ................ 累加这段间隔
                            |
                            +-- 累计 >= 1200 秒 ----> 提醒，全屏遮罩 20 秒，
                                                      按退出码判定有没有照做，
                                                      再提醒，然后清零
```

状态就是 `~/.eye-break/state` 里的三个整数：累计用眼秒数、上次 tick 的时间戳、
已锁屏秒数。判断机器睡没睡过，靠的是 tick 间隔是否超过 `SLEEP_GAP`，不需要额外机制。
每日累计另存在 `~/.eye-break/daily/YYYY-MM-DD`，不受每次提醒和每次锁屏导致的
state 清零影响。

通知由 `EyeBreak.app` 投递，安装时用 `osacompile` 现编。shell 把
`标题|正文|音效` 写进 `~/.eye-break/msg.txt`，再 `open -n` 拉起 App 读文件并投递。

## 几个坑

- **macOS 自带的 bash 是 3.2.57**（2007 年的版本，GPLv3 的历史遗留）。没有 nameref，
  没有关联数组。`local -n` 能通过 `bash -n` 语法检查但运行时才炸，所以要用
  `/bin/bash` 而不是 Homebrew 的 bash 来测。
- 改 applet 的 `Info.plist` 会让代码签名失效，App 会静默拒绝启动。`install.sh`
  改完会重新 ad-hoc 签名。
- 专注模式 / 勿扰会拦掉通知，而且是静默拦掉、还查不出来：`pmset -g assertions`
  不报 DND，`com.apple.ncprefs` 域读不到，`~/Library/DoNotDisturb/DB/` 被 TCC
  挡死。所以「怎么不提醒了」有两种完全不同的成因，靠日志分辨：有
  `notify: start` 说明发了但投递被拦；没有才是计时压根没攒够。全屏遮罩不受
  专注模式影响。
- bash 3.2 解析变量名时不认多字节：`out="$out█"` 会把 UTF-8 的第一个字节吃进
  变量名，配上 `set -u` 直接报 unbound variable。花括号不能省 —— `out="${out}█"`。
- `printf` 补空格是按**字节**而不是显示宽度，中文表头和破折号都会把表格撑歪。
  所以 `--stats` 里每个数据格都只用 ASCII，中文表头是手工对齐的。
- Mac 锁屏但没睡时 launchd 会通宵 tick，所以离屏时长只统计
  `ACTIVE_START`–`ACTIVE_END` 之内的。否则一个四小时的工作日会报出十三小时「离屏」。
- 遮罩要用 Command Line Tools（`xcode-select --install`）编译。没装的话
  `install.sh` 会明说并继续，回落成纯通知。
- 测试钩子：`EYE_BREAK_FAKE_LOCKED`、`EYE_BREAK_FAKE_IDLE`、`EYE_BREAK_LOG`、
  `EYE_BREAK_STATE`、`EYE_BREAK_DAILY`，可以在不碰
  真实数据、不等 20 分钟的前提下把状态机跑一遍。

## 卸载

```sh
./uninstall.sh            # 删掉后台任务和通知器 App
./uninstall.sh --purge    # 连 ~/.eye-break 一起删（统计历史也没了）
```

## 许可

MIT
